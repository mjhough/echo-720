-@videos.each do |video|
  h2=link_to video.title, subject_video_path(video.subject, video)
  h4=link_to video.subject.title, subject_path(video.subject)
  p=truncate(video.description, length: 65)


